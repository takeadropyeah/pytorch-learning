# PyTorch‑Learning 学习笔记 & 实验踩坑总结
本文件记录每一次实验的：
1. 踩坑问题与解决方案
2. 核心知识点总结
3. 课后思考题标准答案
4. 容易混淆的概念对比

---

## exp01 单变量线性回归
### ✅ 实验目的
使用 PyTorch 实现单变量线性回归，模拟 y = wx + b，学习 mini‑batch 训练、模型保存、绘图、设备自动选择。

### ✅ 踩坑记录
- WSL 无法弹出图形窗口，不能用 plt.show()，必须保存图片
- 第一次使用 cuda 出现驱动版本警告：只是警告，不是报错，自动降级 CPU 运行，不影响实验
- loss.item() 必须写，否则保存的是张量，不是数值
- 训练必须 zero_grad() -> forward -> loss.backward() -> step() 固定顺序

### ✅ 核心知识点
- **DataLoader 作用**：自动分 batch、打乱数据、迭代训练
- **梯度清零**：pytorch 梯度会累加，所以每轮必须 zero_grad
- **device 自动迁移**：模型、数据必须在同一设备，否则报错

---

## exp02 MLP 二分类实验
### ✅ 实验目的
搭建双层 MLP 神经网络，学习非线性可分数据，理解 ReLU 激活函数的意义。

### ✅ 踩坑记录
- 二分类 BCELoss **必须配合 Sigmoid**
- 标签维度必须和输出一致，否则 loss 报错
- plt.scatter 画颜色需要 squeeze() 压缩维度
- exp02 网络无 Dropout / BN，所以不需要写 model.train() / eval()

### ✅ 最重要结论
**为什么必须用 ReLU？**
如果只有 Linear 层，无论叠多少层，依然等价单层线性变换，无法拟合环形、非线性数据。
**激活函数给网络引入非线性，才能拟合复杂边界。**

---

## exp03 MNIST 手写数字多分类
### ✅ 实验目的
使用 MLP 完成 10 分类任务，区分训练集/测试集，观察过拟合，掌握 eval、no_grad、准确率计算。

### ✅ 踩坑记录
- 图像维度 [B,1,28,28] 不能直接进全连接，必须 flatten 展平
- 多分类 CrossEntropyLoss **不需要 Softmax**，内部自带
- 测试阶段必须 model.eval() + with torch.no_grad()
- 训练集要 shuffle，测试集不要 shuffle

### ✅ 思考题标准答案
#### 1. 图像为什么要 flatten？
全连接层（Linear）只能接收一维向量，不能处理二维矩阵。因此需要将 28×28 图像展平为 784 维向量。

#### 2. model.eval() 的作用？
将模型切换为评估模式。
作用：关闭 Dropout、冻结 BatchNorm，让网络使用推理行为。
**注意：eval() 不关闭梯度！依然会计算图。**

#### 3. with torch.no_grad() 的作用？
禁用计算图构建，不记录梯度。
测试不需要反向传播，关闭梯度可以节省显存、加速推理、防止内存溢出。

#### 4. 本实验是否出现过拟合？
出现轻微过拟合：
训练 loss 持续下降、训练准确率接近 99.6%，
但测试 loss 在第 6 轮后开始上升、测试准确率不再提升。

#### 5. 缓解过拟合的方法
- 添加 Dropout 层
- 优化器增加 weight_decay 权重衰减
- 提前停止训练
- 数据集增强

---

## ✅ 高频混淆概念
### 1. model.train() vs model.eval()
- **train()**：开启 Dropout、更新 BN 统计量、开启梯度
- **eval()**：关闭 Dropout、冻结 BN、不更新参数
- **eval 不关闭梯度，必须配合 no_grad**

### 2. no_grad 能不能用于训练？
不能。训练需要构建计算图用于反向传播，no_grad 会导致无法更新参数。

### 3. BCE 和 CrossEntropy 的区别
- **二分类：BCE + Sigmoid**
- **多分类：CrossEntropy（无需 Softmax）**

---

## exp04_cnn_mnist 基于CNN的手写数字识别
### ✅实验目的
搭建简单卷积神经网络完成MNIST十分类，对比MLP全连接网络，理解卷积、池化的作用。

### ✅踩坑记录
- 卷积之后feature map尺寸计算出错，导致Linear层输入维度报错；两层2×2最大池化后，28‑>14‑>7，最终是32*7*7。
- 推理脚本里面网络类定义必须和训练脚本完全一模一样，否则load_state_dict加载权重时报错。
- matplotlib保存图片前要先手动建立figures文件夹，目录不存在会保存失败。

### ✅核心知识点
1. Conv2d：卷积核提取图像局部特征（边缘、纹理）；权值共享，参数量相比MLP更小。
2. MaxPool2d最大池化：下采样，降低特征图分辨率，减少计算量，同时保留显著特征。
3. 卷积层输出之后，送入全连接前必须view()展平。

### ✅思考题
#### Q1：为什么CNN在MNIST上效果优于单纯MLP？
MLP把图像直接展平为一维向量，丢失像素之间二维空间的相邻关系；
CNN通过卷积核滑动提取局部空间特征，利用图像的空间先验；并且权值共享减少参数量，降低过拟合风险。

#### Q2：池化层的作用是什么？
降低特征图的空间尺寸，减少后续计算量；对微小位移具有一定的鲁棒性。

#### Q3：对比exp03(MLP)与exp04(CNN)的过拟合倾向？
同等训练轮数下CNN参数量更少并且利用空间信息，相对不容易发生过拟合，测试集准确率更高。

---

## exp05_cnn_regularization CNN加入正则化缓解过拟合
### ✅实验目的
基于exp04的CNN网络，引入Dropout层以及优化器weight‑decay权重衰减(L2正则)，观察正则化抑制过拟合的效果，对比baseline(exp04)结果。

### ✅踩坑记录
1. Dropout **只在model.train()模式下生效；model.eval()推理的时候自动关闭dropout，不需要手动写代码关闭**。如果推理阶段错误保持dropout开启，预测结果会不稳定。
2. weight_decay是优化器的参数，写在Adam里面，不是网络层；它实现L2权重惩罚。
3. 对比观察：加入正则之后训练集准确率相比exp04略有下降属于正常；不能一味追求训练集高分。

### ✅核心知识点
1. Dropout：训练阶段随机将一部分神经元输出置0，防止神经元之间过度协同适应，降低过拟合。
2. weight‑decay(L2正则)：损失增加权重平方项惩罚，约束网络权重不要变得过大。

### ✅思考题
#### Q1：为什么Dropout在推理的时候自动失效？
推理阶段不需要随机失活；如果推理依旧随机失活，每一次跑同一张图片预测结果都会不一样。PyTorch内部根据train()/eval()状态自动控制Dropout行为。

#### Q2：对比exp04和exp05两条测试loss曲线，正则带来什么变化？
exp04后期测试loss明显上升，发生过拟合；exp05使用Dropout+weight‑decay后测试loss后期回升被抑制，泛化性能得到改善；训练集准确率轻微降低。

#### Q3：L2正则（weight‑decay）和Dropout两种正则方式原理上的区别？
weight‑decay(L2)通过修改损失函数，约束权重的数值大小；Dropout通过训练时随机丢弃神经元，破坏部分神经元的协同，属于网络结构层面的正则。

